"""
website python package configuration.
"""

from setuptools import setup

setup(
    name='website',
    version='0.1.0',
    packages=['website'],
    include_package_data=True,
    install_requires=[
    ],
)
